services:
  2fauth:
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          php -r 'exit(@fsockopen("127.0.0.1", 8000) ? 0 : 1);'
      interval: 30s
      timeout: 5s
      retries: 6
      start_period: 20s
